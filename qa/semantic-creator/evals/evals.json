{
  "skill_name": "semantic-creator",
  "evals": [
    {
      "id": 1,
      "name": "grain-first-before-dimensions",
      "prompt": "这是一个 REST 接口：GET /v1/orders 返回 { orders: [ { order_id, store_id, placed_at, total_amount } ] }。帮我建语义层。",
      "expected_output": "先建立事实与原子 grain 决策（一行=一笔订单）；grain 未显式批准时维度决定保持阻塞，不直接生成。",
      "expectations": [
        "Identifies a fact for the orders array before listing dimensions",
        "States a falsifiable grain such as one row per order (orders[] element)",
        "Presents grain as an explicit decision and keeps dependent dimension decisions blocked until it is approved",
        "Does not produce final YAML/OKF before grain is confirmed"
      ],
      "files": []
    },
    {
      "id": 2,
      "name": "no-invented-fields",
      "prompt": "把这个 CLI 转成语义层：`mytool list-invoices --customer` 输出每行 invoice_id, amount, currency。币种字典接口我没给你。",
      "expected_output": "只用给出的字段；缺失的字典来源标 TODO(verify)，不编造 source_operations 或枚举值。",
      "expectations": [
        "Only models fields evidenced by the interface (invoice_id, amount, currency)",
        "Marks the missing currency dictionary source as TODO(verify) instead of inventing an operation",
        "Does not fabricate enum values, codes, or field paths not present in the input",
        "Lists open TODO(verify) items in the deliverable"
      ],
      "files": []
    },
    {
      "id": 3,
      "name": "okf-default-target",
      "prompt": "我有一个 orders 表（order_id PK, store_id FK, placed_at, total_amount）。直接生成语义对象，用默认格式。",
      "expected_output": "默认 Google OKF v0.1 bundle（每概念一个 markdown concept，frontmatter 含 type）；repo-YAML 仅在用户明确要求时；建模仍是 Kimball（grain 先行）。",
      "expectations": [
        "Defaults to a Google OKF v0.1 bundle (concept-per-file markdown with frontmatter type), not repo-YAML",
        "Declares the fact grain (one row per order) before listing dimensions/measures",
        "Models store_id as a dimension reference and order_id as a degenerate dimension",
        "Does not emit repo-YAML unless the user explicitly asks for it"
      ],
      "files": []
    },
    {
      "id": 4,
      "name": "one-blocking-ask-on-grain-ambiguity",
      "prompt": "把 GET /v1/usage 转语义层，响应是 { data: [...] }，结构我暂时说不清。",
      "expected_output": "在改变建模结果的歧义（grain/响应结构未知）处一次一问，给候选；不做问卷连环问，不臆造结构。",
      "expectations": [
        "Asks one blocking question about the unknown response structure / grain",
        "Offers candidate grains or asks for the array element shape",
        "Does not invent a schema or proceed to emit without the structure",
        "Does not fire a long multi-question questionnaire"
      ],
      "files": []
    },
    {
      "id": 5,
      "name": "layer-split-values-to-contract",
      "prompt": "ListProducts 返回 category 字段，枚举有 ELECTRONICS/FOOD/TOYS 等几十个。帮我建语义层维度。",
      "expected_output": "语义层维度只描述形态（kind/business_key/source_operations），枚举取值不内联进语义对象，指向命令/契约层。",
      "expectations": [
        "Models category as a dimension with kind and business_key",
        "Does NOT inline the full enum value list into the semantic object",
        "Points enum/code values to the command/contract layer (notes references contract)",
        "Keeps the semantic layer thin (shape, not concrete typed-in values)"
      ],
      "files": []
    },
    {
      "id": 6,
      "name": "review-report-before-emit",
      "prompt": "接口：GET /v1/orders 返回 { orders: [ { order_id, store_id, total_amount } ] }，GET /v1/stores 返回 { stores: [ { store_id, store_name } ] }。信息都齐了，直接建语义层。",
      "expected_output": "生成对象容器+原子决策的 model JSON 并注入 assets/review-template.html；工作台明确目标/范围/边界，每个低证据或低置信决定给 2–4 个互斥候选及适用条件、收益、代价、风险、证据和推荐理由；grain 未批准时硬阻塞依赖项；所有决定默认 pending，只有用户显式行动后才能 approved:true；反馈支持离线导出/导入并落盘 amendments.md，未批准禁止 Emit。",
      "expectations": [
        "Builds the report by injecting a compact model JSON into the shell template (assets/review-template.html) instead of hand-writing the full HTML",
        "Uses object containers with atomic decisions and keeps basis, confidence, and user status semantically separate",
        "Uncertain decisions offer 2–4 mutually exclusive options with applicability, benefit, cost, risk, evidence references, and recommendation reason",
        "Unresolved depends_on relations hard-block downstream decisions such as dimensions or measures before grain approval",
        "Starts every decision pending; only explicit confirm/select actions can produce approved true, while correct/supplement/reject require another iteration",
        "Decision export is usable via clipboard or offline textarea/download fallback and can be imported on the next iteration",
        "Hard-stops after the report and persists feedback to amendments.md before re-emitting or entering Phase 3"
      ],
      "files": []
    }
  ]
}
