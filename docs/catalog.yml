# Machine-readable skill index. Keep in sync with skills/ and qa/.
version: 1
skills:
  - id: huawei-cloud-billing-scout
    path: skills/huawei-cloud-billing-scout
    qa: qa/huawei-cloud-billing-scout
    display_name: "Huawei Cloud Read-Only Billing — Spend, Charges & Reconciliation"
    display_name_zh: "华为云 · 花多少为何扣 · 只读对账"
    domain: cloud
    subdomains: [billing, finops]
    summary: Read-only Huawei Cloud BSS FinOps—balance, spend, charge attribution, reconciliation, coupons, enterprise billing; one-page briefing; 53 read-only ops; independent skill; refuses pricing-quote (see cost-estimation) and real-name review
    summary_zh: 华为云 · 花多少为何扣 · 只读对账；余额/扣费归因/对账/券包/企业账；一页简报；53 个只读 Operation；独立技能；拒绝询价（见 cost-estimation）/实名
    version: "2.3.9"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: hcloud KooCLI 7.2+, read-only BSS IAM, outbound network; no agent auto-install
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: huawei-cloud-cost-estimation
    path: skills/huawei-cloud-cost-estimation
    qa: qa/huawei-cloud-cost-estimation
    display_name: "Huawei Cloud Cost Estimation & Controlled Provisioning — Quote, Create, Console-Guided Unsubscribe"
    display_name_zh: "华为云成本估算与资源开通"
    domain: cloud
    subdomains: [pricing, finops, procurement, provisioning]
    summary: Huawei Cloud pre-order pricing (period + on-demand; resolve service resource type → ListResourceSpecs → on-demand ListUsageTypes then quote) plus 74 controlled create ops; small chains complete directly, larger chains 2-3 commands/round; creates require help, --dryrun, fee review, confirmation; unsubscribe console-only
    summary_zh: 华为云 · 询价（资源类型→规格→按需 usage_factor→报价）+ 受控开通 + 退订控制台指引；小任务直达、大任务每轮 2–3 条；开通必经 help/--dryrun/费用/确认
    version: "3.2.4"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: hcloud KooCLI 7.2+, IAM permissions matching requested read/create operations, outbound network; no agent auto-install
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: huawei-cloud-account-onboarding
    path: skills/huawei-cloud-account-onboarding
    qa: qa/huawei-cloud-account-onboarding
    display_name: "Huawei Cloud Account Onboarding — Real-Name Verification via Face-Scan QR"
    display_name_zh: "华为云账号开通 · 人脸扫码实名认证"
    domain: cloud
    subdomains: [onboarding, identity]
    summary: Read-only Huawei Cloud real-name verification guidance via hcloud BSS—check verified_status, and when unverified fetch the face-auth QR address, render it in the terminal to scan, then poll with --cli-waiter until verification lands; face-scan channel only; refuses ID/document/bank-card intake, write operations, and non-Huawei-Cloud identity flows
    summary_zh: 华为云 · 人脸扫码实名认证引导；查状态 → 取二维码地址 → 终端渲染 → waiter 轮询至已实名；仅人脸通道，只读；拒收证件/银行卡、拒绝写接口；企业与证件认证指路控制台
    version: "1.0.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: hcloud KooCLI 7.2+ with a configured profile (main account), Node.js 18+ for the QR renderer, outbound network; no agent auto-install
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: semantic-km-creator
    path: skills/semantic-km-creator
    qa: qa/semantic-km-creator
    display_name: "Semantic KM Creator — Enterprise Knowledge from APIs & Databases"
    display_name_zh: "语义 · 企业知识 · 接口/库转语义层"
    domain: meta
    subdomains: [semantic-layer, dimensional-modeling, ontology, okf, enterprise-knowledge]
    summary: Enterprise knowledge—turns a REST/OpenAPI, CLI, or table/DDL interface into a governed Kimball semantic layer through an interactive HTML decision workbench with fixed YAGNI guidance, atomic decisions, explicit options, evidence, risks, dependencies, and user approval; emits Google OKF v0.1 by default or repo YAML; evidence-only; counterpart to semantic-pkm-creator
    summary_zh: 企业知识 · 把 REST/CLI/数据表接口经访谈与 HTML 决策工作台建成 Kimball 语义层；四类固定 YAGNI 指导帮助理解原子决定、候选、证据、风险、依赖与权衡，用户显式批准后输出；默认 Google OKF v0.1，可选仓库 YAML；不臆造；个人知识库见 semantic-pkm-creator
    version: "0.6.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: No external tools or network; pure modeling + file generation; review report is self-contained HTML (inline CSS/JS, no CDN)
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: semantic-pkm-creator
    path: skills/semantic-pkm-creator
    qa: qa/semantic-pkm-creator
    display_name: "Semantic PKM Creator — Scene, Concept, Entity Extract"
    display_name_zh: "语义 · 个人知识管理 · 场景-概念-实体萃取"
    domain: meta
    subdomains: [knowledge-extraction, ontology, sce]
    summary: Personal knowledge management—two-round extract of callable scenes, concepts, and entities from source text—skeleton first with human confirm, then IPO/decompose/assemble and eight relation types; concept is the hub
    summary_zh: 个人知识管理 · 两轮从原文萃取可调用的场景、概念与实体：先扫骨架并确认，再回填 IPO/分解/组装与八种关系；概念是中枢
    version: "0.3.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: No external tools or network; writes three YAML files plus a round-1 progress note; relation reference loaded on demand
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: concept-design
    path: skills/concept-design
    qa: qa/concept-design
    display_name: "Concept Design"
    display_name_zh: "概念设计"
    domain: meta
    subdomains: [concept-design, requirements, software-design]
    summary: Explicit-invocation-only — models requirements as independent concepts defined by one purpose, an end-to-end operational principle, relational state, and actions with error cases and read-only queries; critiques boundaries through specificity, completeness, independence, and familiarity; composes application behavior with when/where/then causal synchronizations (the author's current notation) and follows the requested handoff scope
    summary_zh: 仅显式调用 · 用单一 Purpose、端到端 Operational Principle、关系式 State、带错误 case 与只读 queries 的 Actions 建模独立 concepts；以专一、完整、独立、熟悉审查边界，用 when/where/then 因果 synchronizations（作者现行记法）组合应用行为，按用户授权范围交接
    version: "0.6.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: No external tools or network; pure concept modeling
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: concept-prd
    path: skills/concept-prd
    qa: qa/concept-prd
    display_name: "Concept PRD"
    display_name_zh: "概念 PRD"
    domain: meta
    subdomains: [concept-design, prd, specifications]
    summary: Explicit-invocation-only — transcribes a confirmed concept model (Daniel Jackson's concept design) into a PRD family—central overall PRD plus Jackson-style per-concept CONCEPT.md (colocated with code when modules exist) and a single flow-grouped SYNCS.md (split per sync package later by implementation)—without inventing content; zero cross-concept naming in concept specs; model gaps return to concept design
    summary_zh: 仅显式调用 · 把已确认的概念模型（Daniel Jackson 概念设计）转录为 PRD 文档族：总体 PRD 集中，每概念 CONCEPT.md 与代码共存（Jackson 格式）、全部 sync 转录期单文件按 flow 分组（实现期拆包归实现技能）；概念规格零点名，纯转录不再创作，缺口回上游建模
    version: "0.5.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: No external tools or network; writes Markdown PRD files
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: concept-implementation
    path: skills/concept-implementation
    qa: qa/concept-implementation
    display_name: "Concept Implementation — Modular Monolith"
    display_name_zh: "概念实现 · 模块单体"
    domain: meta
    subdomains: [concept-design, modular-monolith, implementation]
    summary: Explicit-invocation-only — maps a confirmed concept model (Daniel Jackson's concept design) onto a modular monolith—one module per concept, when/where/then syncs as mediators or a rule engine with error syncs as the default failure path, specs colocated with code, validated product subsets; scales via semantics-free concept grouping and per-group sync packages; language notes for Rust, Java/Spring Modulith, and TypeScript
    summary_zh: 仅显式调用 · 把已确认的概念模型（Daniel Jackson 概念设计）映射为模块单体：一个 concept 一个模块，when/where/then sync 落在组合层（mediator 或规则引擎、错误 sync 为默认失败路径），规格与代码共存，验证产品子集与构建支持；规模大时按无语义概念分组与 sync 拆包扩展；按需加载 Rust / Java / TypeScript 说明
    version: "0.6.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: Mapping needs only the confirmed model; language-specific boundary tests may use cargo, Spring Modulith, or dependency-cruiser
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: concept-audit
    path: skills/concept-audit
    qa: qa/concept-audit
    display_name: "Concept Audit"
    display_name_zh: "概念审计"
    domain: meta
    subdomains: [concept-design, architecture-audit, spec-drift]
    summary: Explicit-invocation-only — read-only audit of a codebase against its concept model (Daniel Jackson's concept design) around two questions—are concepts truly independent, and is their sync composition free of defects—plus spec drift with calibrated checklists and cross-spec validation (adapted from jlifyio/wyx), criteria review, and dependency/subset integrity; parallel scans for 5+ specs; evidenced findings routed to design, prd, or implementation
    summary_zh: 仅显式调用 · 只读对照概念规格与代码，回答两问：概念是否真独立、组合是否有缺陷（行为保持、错误 sync、入口响应、冲突/死 sync、级联、欠/过同步、直通动作）；加规格漂移（校准检查表与跨规格校验，改编自 jlifyio/wyx）、判据重审、依赖与子集；规格多时并行扫描，发现带证据并路由到 design / prd / implementation
    version: "0.5.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: Read-only; needs the codebase plus optional PRD or concept-model docs
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: concept-guardrails
    path: skills/concept-guardrails
    qa: qa/concept-guardrails
    display_name: "Concept Guardrails"
    display_name_zh: "概念护栏"
    domain: meta
    subdomains: [architecture, module-boundaries, drift-detection, concept-specs]
    summary: Explicit-invocation-only — six modes (audit, concept, drift, pipeline, sync, map) that declare module boundaries as colocated CONCEPT/PIPELINE/SYNCS specs in this repo's Jackson notation only, audit spec coverage with dependency-ordered command plans, detect spec-code drift with impact-calibrated severities and cross-spec reference validation, and render an ARCHITECTURE.md Mermaid map; workflow adapted from jlifyio/wyx v0.26.0, whose native format is flagged for migration rather than consumed; upstream Claude Code hooks runtime kept as an optional spec-listing layer
    summary_zh: 仅显式调用 · 六种模式（audit、concept、drift、pipeline、sync、map）用与代码同目录的 CONCEPT/PIPELINE/SYNCS 规格声明模块边界，只消费本仓 Jackson 记法；审计覆盖缺口并按依赖排序给出命令计划；用校准过的严重度与跨规格校验检测规格漂移；生成 ARCHITECTURE.md Mermaid 地图；流程改编自 jlifyio/wyx v0.26.0，其原生格式只标待迁移不消费；上游 hooks 运行时保留为可选的规格列举层
    version: "0.30.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: Specs and drift checks need only file read/write plus Grep/Glob; the optional boundary-injection runtime requires Claude Code hooks and jq
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
    upstream: https://github.com/jlifyio/wyx (MIT; notice retained as skills/concept-guardrails/LICENSE.upstream)
  - id: html-slides
    path: skills/html-slides
    qa: qa/html-slides
    display_name: "HTML Slides — Styled Single-File Decks (Huawei / Apple / Generic)"
    display_name_zh: "HTML 胶片 · 华为 / Apple / 通用风格单文件 HTML 演示"
    domain: content
    subdomains: [presentation, slides, huawei-style, html-deck]
    summary: Explicit-invocation-only deck builder—settles scene, audience, duration and style, confirms an outline then a per-page table, and writes a plain single-file 1920×1080 HTML (inlined vars + base.css + runtime.js, one section per page, data-URI images, ?debug overflow check) in a pluggable style (generic, huawei official light/dark with bundled assets, apple); optional screenshot PPTX via tools/export_pptx.py or python-pptx rebuild
    summary_zh: 仅显式调用的胶片生成器 · 先定场景 / 对象 / 时长 / 风格，确认大纲再确认逐页表，然后写一份明文单文件 1920×1080 HTML（内联变量块 + base.css + runtime.js，每页一个 section，图片 data URI，?debug 查溢出）；风格插拔：generic、huawei 官方浅 / 深色（随包素材）、apple；可选截图版 PPTX 或 python-pptx 重建
    version: "0.1.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: Writing the deck needs only file read/write; optional tools/export_pptx.py needs Python 3, playwright (Chromium) and python-pptx, never auto-installed
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
    notes: Huawei assets under assets/huawei are Huawei trademarks bundled for Huawei-style decks only
