# Machine-readable skill index. Keep in sync with skills/ and qa/.
version: 1
skills:
  - id: huawei-cloud-billing-scout
    path: skills/huawei-cloud-billing-scout
    qa: qa/huawei-cloud-billing-scout
    display_name: "Huawei Cloud Read-Only Billing — Spend, Charges & Reconciliation"
    display_name_zh: "华为云 · 花多少为何扣 · 只读对账"
    domain: cloud
    subdomains: [billing, finops]
    summary: Read-only Huawei Cloud BSS FinOps—balance, spend, charge attribution, reconciliation, coupons, enterprise billing; one-page briefing; 53 read-only ops; independent skill; refuses pricing-quote (see cost-estimation) and real-name review
    summary_zh: 华为云 · 花多少为何扣 · 只读对账；余额/扣费归因/对账/券包/企业账；一页简报；53 个只读 Operation；独立技能；拒绝询价（见 cost-estimation）/实名
    version: "2.3.9"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: hcloud KooCLI 7.2+, read-only BSS IAM, outbound network; no agent auto-install
    marketplaces: [skills-sh, skillsmp, clawhub]
    distribution: direct-skill
    clawhub_license: MIT-0 on ClawHub; repository source remains Apache-2.0
  - id: huawei-cloud-cost-estimation
    path: skills/huawei-cloud-cost-estimation
    qa: qa/huawei-cloud-cost-estimation
    display_name: "Huawei Cloud Cost Estimation & Controlled Provisioning — Quote, Create, Console-Guided Unsubscribe"
    display_name_zh: "华为云成本估算与资源开通"
    domain: cloud
    subdomains: [pricing, finops, procurement, provisioning]
    summary: Huawei Cloud pre-order pricing (period + on-demand, specs resolved live via ListResourceSpecs) plus 74 controlled create ops, including CodeArts IDE Online; creates require runtime help, mandatory --dryrun, fee review and explicit confirmation; unsubscribe is console-guidance-only with no CLI/API; fail-fast batches, no auto-rollback; refuses credential intake, payment/renewal/delete, non-allowlisted writes, cross-cloud
    summary_zh: 华为云 · 询价 + 受控开通 + 退订控制台指引；包年包月与按需询价、规格实查；74 个白名单开通命令（含 CodeArts IDE Online）必经运行时 help、强制 --dryrun、费用回表与显式确认；退订不调用或输出 CLI/API，仅引导官网控制台核对资源、关联项与退款；批次失败即停不自动回滚
    version: "3.1.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: hcloud KooCLI 7.2+, IAM permissions matching requested read/create operations, outbound network; no agent auto-install
    marketplaces: [skills-sh, skillsmp]
    distribution: direct-skill
  - id: huawei-cloud-account-onboarding
    path: skills/huawei-cloud-account-onboarding
    qa: qa/huawei-cloud-account-onboarding
    display_name: "Huawei Cloud Account Onboarding — Real-Name Verification via QR (Mock)"
    display_name_zh: "华为云账号开通 · 扫码实名认证（Mock）"
    domain: cloud
    subdomains: [onboarding, identity]
    summary: Guides Huawei Cloud real-name verification via terminal QR—check account status, generate scan-to-verify QR (LAN H5 page), poll every 3s to approved/rejected/expired; local mock modeled on industry create-session/poll shape; refuses real ID document or credential intake and non-Huawei-Cloud identity flows
    summary_zh: 华为云 · 扫码实名认证引导；查状态 → 终端二维码 → 3s 轮询收敛；本地 mock（业界三段式接口形态）；拒收真实证件/凭证；仅华为云场景
    version: "0.1.0"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: Node.js 18+, phone on same WiFi to scan; local mock, no Huawei Cloud credentials needed
    marketplaces: []
    distribution: direct-skill
  - id: semantic-creator
    path: skills/semantic-creator
    qa: qa/semantic-creator
    display_name: "Semantic Creator — Interface to Governed Semantic Layer (Meta-Skill)"
    display_name_zh: "语义创建器 · 接口转语义元技能"
    domain: meta
    subdomains: [semantic-layer, dimensional-modeling, ontology, okf]
    summary: Meta-skill—turns a REST/OpenAPI, CLI, or table/DDL interface into a governed Kimball semantic layer through an interactive HTML decision workbench with fixed YAGNI guidance, atomic decisions, explicit options, evidence, risks, dependencies, and user approval; emits Google OKF v0.1 by default or repo YAML; evidence-only
    summary_zh: 元技能 · 把 REST/CLI/数据表接口经访谈与 HTML 决策工作台建成 Kimball 语义层；四类固定 YAGNI 指导帮助理解原子决定、候选、证据、风险、依赖与权衡，用户显式批准后输出；默认 Google OKF v0.1，可选仓库 YAML；不臆造
    version: "0.5.1"
    agents: [cursor, claude-code, codex, openclaw]
    compatibility: No external tools or network; pure modeling + file generation; review report is self-contained HTML (inline CSS/JS, no CDN)
    marketplaces: [skills-sh, skillsmp]
    distribution: direct-skill
