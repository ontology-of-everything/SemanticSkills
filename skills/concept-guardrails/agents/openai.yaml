interface:
  display_name: "概念护栏（Concept Guardrails）"
  short_description: "用 CONCEPT/PIPELINE/SYNCS 规格划定模块边界并检测规格漂移"
  default_prompt: "Use $concept-guardrails <audit|concept|drift|pipeline|sync|map> to declare this module's boundaries as a concept spec, or to check specs against code for drift."
policy:
  allow_implicit_invocation: false
