interface:
  display_name: "wyx 架构护栏（中文版）"
  short_description: "用 CONCEPT/PIPELINE/SYNCS 规格划定模块边界并检测规格漂移"
  default_prompt: "Use $wyx-zh-cn to declare this module's boundaries as a wyx spec, or to check specs against code for drift."
policy:
  allow_implicit_invocation: false
