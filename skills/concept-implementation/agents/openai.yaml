interface:
  display_name: "Concept Implementation"
  short_description: "把概念模型映射为模块单体代码：concept→模块、sync→mediator"
  default_prompt: "Use $concept-implementation to map the confirmed concept model into a modular monolith: one module per concept, syncs as mediators, validate product subsets and implementation boundaries."
policy:
  allow_implicit_invocation: false
