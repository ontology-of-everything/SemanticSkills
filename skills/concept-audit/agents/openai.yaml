interface:
  display_name: "Concept Audit"
  short_description: "五维审计存量工程：漂移、边界、判据、组合、依赖，只读输出修复路由"
  default_prompt: "Use $concept-audit to audit the codebase against its concept model: spec drift, boundary violations, criteria review, composition issues, and dependency rules. Read-only; output findings with fix routing."
